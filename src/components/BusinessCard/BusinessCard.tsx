import React, { useCallback, useEffect } from "react"
import Image from "next/image"

export interface IBaseModal extends React.HTMLAttributes<HTMLDivElement> {
  visible: boolean
}

const BusinessCard = ({ visible }: IBaseModal) => {
  return <div>fwef</div>
}

export default BusinessCard
